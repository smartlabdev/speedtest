# Speedtest Server — Konfigurasjon
# Kopier denne filen til config.env og juster verdiene.
# Start med: docker-compose --env-file config/config.env up -d

PORT=8080
MAX_SIZE_MB=100
SERVER_NAME=Heimnett SpeedTest